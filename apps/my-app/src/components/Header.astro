---
interface Props {
    title?: string;
}

const { title } = Astro.props;

---

<header>
    {title && <h1>{title}</h1>}
    <slot />
</header>

<style>
    header {
        display: flex;
    }
</style>